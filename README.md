# 孙茜 · 作品集（网页版 · 视觉设计师版）

把 `孙茜-视觉设计师作品集`（19 页）做成的可分享网页。设计稿原样呈现，外面套了一层真正的网页外壳：
固定章节导航、滚动进度、封面解锁动效、点击放大看细节、手机端适配。

> **本仓库是「视觉设计师」版**，线上 <https://sunqian321.github.io/portfolio-visual/>。
> 投 UI 设计师岗的那版在另一个仓库 `portfolio`（<https://sunqian321.github.io/portfolio/>），
> 两者的 PDF、文案各自独立，互不影响。多版本怎么维护见下面专门一节。

**每张图导出三档宽度（1440 / 1920 / 2880），浏览器按自己的屏幕挑一档**——
手机下 1440 档（省流量、解码快），Retina 笔记本下 2880 档（不糊）。
配合按章节懒加载，**首屏只加载 202 KB**，滚到哪才下哪。

---

## 目录结构

```
网页版-视觉版/
├── index.html                  ← 页面骨架（导航、各章节、联系区）
├── .nojekyll                   ← GitHub Pages 需要，别删
├── assets/
│   ├── css/style.css
│   ├── js/main.js              ← 交互逻辑
│   ├── js/manifest.js          ← 自动生成，别手改
│   └── img/                    ← 自动生成，别手改
│       ├── ch0/  封面、关于我、目录、尾页
│       ├── ch1/  01 APP&AI智能体（23 张切片）
│       ├── ch2/  02 数据可视化（8 张）
│       ├── ch3/  03 图标&字体（4 张）
│       ├── ch4/  04 平面&电商（23 张）
│       │         └─ 每个名字都有 -1440 / -1920 / -2880 三个文件
│       └── og-cover.jpg        ← 链接分享出去的预览卡（尺寸只有一份）
└── tools/
    ├── build_tiles.py          ← 图片导出脚本
    └── check_pdf.py            ← 换 PDF 前先验清晰度
```

---

## 本地预览

**别直接双击 `index.html`**，那样是 `file://` 协议，懒加载和相对路径的表现和线上不一致。起个本地服务器：

```bash
cd "/Users/zaizai/Documents/作品集/网页版-视觉版"
python3 -m http.server 8765
```

然后浏览器打开 <http://localhost:8765>

---

## 改了设计稿之后怎么更新

1. 确认新 PDF 够清晰（**换之前必须跑**，压缩导出会让画面发糊）：

```bash
python3 tools/check_pdf.py 新的.pdf        # 不给路径就用脚本里配的那份
```

   看到 `✅ 全部页面的图都够清晰` 再往下走。
2. 重跑导出（约 80 秒，会先清空旧图重建。慢在三档 WebP 编码，`method=6` 是拿时间换体积）：

```bash
python3 tools/build_tiles.py
```

3. 如果**页数或页面顺序变了**，还要同步改 `tools/build_tiles.py` 里的 `PAGES` 表——
   它规定了「第几页属于哪个章节、哪几页单独成图、哪几页进长图序列」。
4. 本地看过没问题，再提交推送（见下）。

### 想调画质或体积

改 `tools/build_tiles.py` 顶部参数后重跑：

| 参数 | 现在 | 说明 |
|---|---|---|
| `MASTER_SCALE` | `1.5` | 母版 2880px 宽。**不能低于 1.5**——Retina 屏（1440 CSS px × 2 倍像素）要的就是 2880px，母版更小的话浏览器只能靠插值放大，画面发糊 |
| `TIER_WIDTHS` | `[1440,1920,2880]` | 导出三档，浏览器自己挑。加档位 = 加仓库体积，一般不用动 |
| `WEBP_QUALITY` | `80` | UI 界面小字多，q76 会把笔画压糊，别再往下降 |
| `TILE_H` | `3000` | 切片高度（母版像素）。2880×3000 = 8.6M 像素，远低于 iOS Safari 约 16M 的解码上限 |

改完重跑，脚本会先清空 `assets/img/` 再重建，**仓库图片总量 38.7 MB**，分布：

| 档位 | 体积 | 谁在用 |
|---|---|---|
| 1440 | 7.9 MB | 手机、普通笔记本 |
| 1920 | 11.5 MB | 1080p 台式 |
| 2880 | 19.2 MB | Retina 屏 ← 真正清晰的这档 |

> 也测过 AVIF：同样的观感只小 8%，不值得多维护一套文件，故未采用。

---

## 多版本怎么维护

同一套外壳代码服务多个求职方向，**一个版本 = 一个仓库 = 一个网址**，互不影响：

| 仓库 | 网址 | 面向 |
|---|---|---|
| `portfolio` | https://sunqian321.github.io/portfolio/ | UI 设计师岗 |
| `portfolio-visual` | https://sunqian321.github.io/portfolio-visual/ | 视觉设计师岗 |

版本之间**只差两样东西**：源 PDF，和 `index.html` 里的身份文案。所以新增一版：

```bash
cp -R 网页版-视觉版 网页版-新方向
cd 网页版-新方向
rm -rf assets .git                    # 图重出、git 重建
# 改 index.html 的 5 处身份文案，改 build_tiles.py 里 SRC_PDF 的默认值
python3 tools/build_tiles.py          # 约 80 秒
git init && git add -A && git commit -m "初版"
```

`index.html` 要改的 5 处：`<title>`、`meta description`、`og:title`、
关于我那张图的 `alt`、页脚。**搜「设计师」三个字就能全部找到**：

```bash
grep -n 设计师 index.html
```

`build_tiles.py` 的 `SRC_PDF` 支持命令行覆盖，临时换 PDF 不用改代码：

```bash
python3 tools/build_tiles.py /path/to/另一版.pdf
```

> ⚠️ 这些仓库都是**公开**的 —— GitHub 免费版的 Pages 要求仓库公开，
> 所以从 `github.com/sunqian321` 能互相翻到。如果以后需要版本之间互相看不见，
> 要么给仓库起中性名（`portfolio`、`portfolio-2`），
> 要么把 Pages 迁到 Cloudflare Pages / Vercel（免费支持私有仓库）。

---

## 部署到 GitHub Pages

```bash
git add -A
git commit -m "更新作品集"
git push
```

推完后 GitHub 会自动重新发布，约 1 分钟生效。

**首次部署**需要在 GitHub 仓库里：`Settings → Pages → Source 选 main 分支 → Save`。

---

## 为什么锚点跳转不是「平滑滚动」

整页十万像素高（桌面端 `scrollHeight` 约 106,000px）。如果按常规写
`html { scroll-behavior: smooth }`，点一次导航「联系我」会变成**横穿全页、
持续约 60 秒的动画**，沿途还要把 58 张图、11 MB 全部下载解码一遍。
实测过这个数字，弱显卡的 Windows 上就是一路卡过去。

所以 `main.js` 的 `goTo()` 按距离分流：

| 距离 | 行为 |
|---|---|
| ≤ 2 屏 | 平滑滚动（60fps 动画只要几百毫秒，观感好） |
| > 2 屏 | 直接瞬移（0.33 秒到位，沿途图片根本不触发下载） |

`prefers-reduced-motion` 的用户一律瞬移。

## 性能上的几处刻意取舍

这些改动都是为了弱显卡机器（尤其 Windows 核显），在本机强 GPU 上几乎看不出差别，
所以在弱机器上出问题前不容易发现。改之前先看一眼：

| 位置 | 做法 | 原因 |
|---|---|---|
| `.nav` | 用不透明深色底，**不用 `backdrop-filter`** | 毛玻璃滚动时每帧都要重新采样背后的画面，是滚动卡顿的常见来源 |
| `.progress` | `transform: scaleX()`，**不用 `width`** | `width` 每帧触发布局+重绘；`transform` 只走合成层 |
| `.unlock-ring::after` | 脉冲动画只动 `transform`/`opacity` | 原来直接动画 `box-shadow`，每帧重绘且 `infinite` 永不停止 |
| `main.js` 的 `measure()` | 缓存 `offsetTop` / `scrollHeight` | 读这些属性会强制同步布局。以前放在每帧的 `update()` 里，等于每帧重新布局一次。现在只在 `resize` 和 `ResizeObserver` 触发时重量 |

> 顺带一提：右侧那排章节圆点（`.rail`）已按用户要求移除，它是固定定位 + 毛玻璃，
> 既挡视线也在滚动时要额外合成。

## 已知限制

- **图中的文字不能选中、不能复制、搜索引擎读不到**。因为整站是设计稿导出的图片。
  如果需要可搜可复制，得把页面改写成真正的 HTML 排版，那是另一套做法。
- **国内访问速度不稳定**。GitHub Pages 服务器在海外。免费又国内快的方案在 2026 年基本不存在
  （Gitee Pages 已停服；腾讯云 EdgeOne 国内节点需 ICP 备案，7–20 个工作日）。
  缓解手段是首屏压到 202 KB、每张图按屏幕挑档位、按章节懒加载。
- **仓库 38.7 MB**（2026-09 从 9.7 MB 提上来的）。之前只导 1920 一档，Retina 屏上必然发糊；
  现在每张图存三档，其中 2880 档独占 19.2 MB。真正下载到单个访问者机器上的只有其中一档
  （手机 1440 那 7.9 MB 里的若干张）。

  **但这 39.4 MB 不是「打开卡」的原因**——实测过一次访问最多只下 1～2 MB。真凶是每帧的
  计算量，见上面「性能上的几处刻意取舍」。

  真要压体积，只有三条路，每条都有明确代价：
  | 做法 | 省 | 代价 |
  |---|---|---|
  | 去掉 2880 档 | 19.8 MB | Retina Mac 退回 1920，**发糊**（就是最初那个问题） |
  | 去掉 1920 档 | 11.6 MB | 1080p 台式退回 1440，**发糊**（Windows 大多数是这种屏） |
  | 换 AVIF | 约 30% | 解码更慢，**弱机器更卡**，方向反了 |

  画质和体积在这条路上是直接对冲的，没有免费午餐。
- **手机端**：16:9 的画面在手机屏幕上会缩小，字看不清。点一下画面可以全屏放大、双指缩放。
- **不含 PDF 下载**。原 PDF 有 39MB，放进仓库会让每次推送都多传 39MB，且以后每换一版
  都会在 git 历史里永久留一份。现已从仓库排除（`.gitignore` 里已屏蔽），
  文件仍留在本地目录里。若以后确实需要，建议放网盘、把链接加到联系区。
